import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { SkipLink } from './';
import { Grid } from '../Grid';

<Meta title="Components/SkipLink" component={SkipLink} />

# SkipLink

The `<SkipLink/>` component.

## Import

```js
import { SkipLink } from '@firstclasspostcodes/sw14';
// or
import { SkipLink } from '@firstclasspostcodes/sw14/lib/components/SkipLink';
```

<Preview>
  <Story name="SkipLink">
    <>
      <p>
        To view the skip link component tab to this example, or click inside this example and press tab.
      </p>
      <SkipLink href="#" />
    </>
  </Story>
</Preview>