import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { Button } from '.';

<Meta title="Components/Button" component={Button} />

# Button

The `<Button/>` component is used for `<button/>` elements. It uses the `Typography.Text` component to style the look of the text inside the element. 

## Import

```js
import { Button } from '@firstclasspostcodes/sw14';
// or
import { Button } from '@firstclasspostcodes/sw14/lib/components/Button';
```

## Presets

<Preview>
  <Story name="Presets/Standard">
    <Button>Standard</Button>
  </Story>
  <Story name="Presets/Primary">
    <Button.Primary>Primary</Button.Primary>
  </Story>
  <Story name="Presets/Secondary">
    <Button.Secondary>Secondary</Button.Secondary>
  </Story>
  <Story name="Presets/Success">
    <Button.Success>Success</Button.Success>
  </Story>
  <Story name="Presets/Warning">
    <Button.Warning>Warning</Button.Warning>
  </Story>
  <Story name="Presets/Error">
    <Button.Error>Error</Button.Error>
  </Story>
</Preview>

## Larger & Colored

<Preview>
  <Story name="Larger & Colored">
    <Button color="brand" size="20">Some button</Button>
  </Story>
</Preview>

## Background

<Preview>
  <Story name="Background">
    <Button background="brand" color="white">Some button</Button>
  </Story>
</Preview>

## Text

<Preview>
  <Story name="Text">
    <Button border={false} color="brand">Button without a border</Button>
  </Story>
</Preview>